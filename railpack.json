{
    "$schema": "https://schema.railpack.com",
    "steps": {
        "install": {
            "commands": ["npm run server:install"]
        }
    },
    "deploy": {
        "startCommand": "npm run server:start"
    }
}
